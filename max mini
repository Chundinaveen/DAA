#include<stdio.h>
int main()
{
	int a[]={10,20,30,40,50},i,max=a[0],min=a[0];
	for(i=0;i<5;++i)
	{
		if (a[i]<min)
		{
			min=a[i];
		}
		if (a[i]>max)
		{
			max=a[i];
		}
	}
    printf("Print the value of minimum:%d",min);
    printf("\nPrint the value if maximum:%d",max);
    return 0;
}
